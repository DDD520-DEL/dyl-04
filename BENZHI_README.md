基于 Go 实现的分布式任务调度引擎项目，一款后端服务，完成定时任务注册、优先级排队、worker 租约与执行重试管理。

## 功能

- 任务注册：一次性任务与 cron 任务，支持优先级、超时与最大重试次数
- 调度执行：到期扫描、手动立即触发、取消与 cron 更新
- 队列机制：优先队列与延迟重试队列
- 租约机制：worker 注册、心跳续期、超时接管
- 执行记录：成功/失败落库、重试与死信流转

## 构建与运行

```bash
go build ./...
go vet ./...
go test ./...
go run ./cmd/schedd
```

服务默认监听 :8090，提供 `/api/tasks/*` 与 `/api/workers/*` HTTP 接口。
